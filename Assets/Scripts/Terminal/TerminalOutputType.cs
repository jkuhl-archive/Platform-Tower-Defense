namespace Terminal
{
    /// <summary>
    ///     Enum for storing the types of messages that can be printed to the terminal output
    /// </summary>
    public enum TerminalOutputType
    {
        Error,
        Header,
        Info,
        InputRecord
    }
}